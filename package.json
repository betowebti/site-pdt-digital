{
  "name": "pdt-sp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/generate_definitions.js && next dev",
    "build": "node scripts/generate_definitions.js && next build && next export",
    "start": "node scripts/generate_definitions.js && next start"
  },
  "dependencies": {
    "bootstrap": "^4.6.0",
    "next": "10.1.3",
    "react": "17.0.2",
    "react-bootstrap": "^1.5.2",
    "react-dom": "17.0.2",
    "sass": "^1.32.10"
  }
}
